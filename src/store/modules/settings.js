// initial state
const state = {
  theaterIds: [],
};

// actions
const actions = {
};

// mutations
const mutations = {
};

// getters
const getters = {
};

export default { state, actions, mutations, getters };
